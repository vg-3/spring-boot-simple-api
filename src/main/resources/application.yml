server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    username: varshithgowdav
    password: Leela@1422
    url: jdbc:postgresql://localhost:5432/customer
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  main:
    web-application-type: servlet